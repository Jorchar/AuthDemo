meta {
  name: create user
  type: http
  seq: 1
}

post {
  url: {{BaseUrl}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "firstname":"Jordan",
    "lastname":"Kucharski"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
